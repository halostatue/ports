# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           golang 1.0

go.setup            github.com/chainloop-dev/chainloop 1.81.2 v
revision            0

categories          security
maintainers         {macports.halostatue.ca:austin @halostatue} \
                    openmaintainer
license             Apache-2

description         ${distname} is an open source software supply chain control plane

long_description    {*}${description}, a single source of truth for artifacts \
                    plus a declarative attestation crafting process.

checksums           ${distname}${extract.suffix} \
                    rmd160  f92831a9bcd7e3dc1f00493e7c8ce4ec577cfdb4 \
                    sha256  c5bc4c34f10d37c04309d27eb8facd5bb9f2d64d273cc15a86de7489672135e3 \
                    size    7236240

destroot {
    xinstall -m 0755 ${worksrcpath}/${name} ${destroot}${prefix}/bin/
}

go.offline_build    no

build.pre_args-append   -ldflags \
                        \"-X ${go.package}/app/cli/cmd.Version=${github.tag_prefix}${version}" \
                        -o ${worksrcpath}/${name}
build.args              ./app/cli
